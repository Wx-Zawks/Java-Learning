<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demo.zhouzhou.mapper.StudentMapper">
<!--    <update id="updateClazz">-->
<!--        update clazz-->
<!--        <set>-->
<!--            <if test="name != null and name != ''">name = #{name},</if>-->
<!--            <if test="room != null and room != ''">room = #{room},</if>-->
<!--            <if test="beginDate != null">begin_date = #{beginDate},</if>-->
<!--            <if test="endDate != null">end_date = #{endDate},</if>-->
<!--            <if test="masterId != null">master_id = #{masterId},</if>-->
<!--            <if test="subject != null">subject = #{subject}</if>-->
<!--        </set>-->
<!--        where id = #{id}-->
<!--    </update>-->
    <select id="selectStudents" resultType="demo.zhouzhou.pojo.Student">
        select s.*,c.name as clazzName from student s left join clazz c on s.clazz_id = c.id
        <where>
            <if test="name != null and name != '' ">
                s.name like concat('%', #{name}, '%')
            </if>
            <if test="clazzId != null">
                and s.clazz_id = #{clazzId}
            </if>
            <if test="degree != null">
                and s.degree = #{degree}
            </if>
        </where>
        order by s.update_time desc
    </select>
</mapper>